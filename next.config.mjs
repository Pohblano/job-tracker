/**
 * Next.js configuration for the Shop Visibility Board; keeps App Router defaults and typed routes for safer TV/admin pages.
 */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    typedRoutes: true,
  },
};

export default nextConfig;
